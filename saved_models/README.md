# saved_models/

Serialized, trained model artifacts (`.joblib`), produced by scripts in
`ml/` and loaded at runtime by the Streamlit app. Not committed to git —
regenerate with `python ml/train_heart_model.py` (added in Milestone 4).
